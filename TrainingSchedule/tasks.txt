General todos:
- Format checks for inputs (Weekday, km, duration, etc.)
- Change color os row when completed (any of the planned fields are filled)
- A way to re-order goals, maybe just order by planned day

Missing features:
- Templates

Improvements:
- xml header
- change week number by scrolling and hovering over a week
- When an item is deleted from a list or table, select the below items